namespace TeaSpoons.AddressablesToolbox.Tests
{
    using UnityEngine;

    /// <summary>
    /// A component for the test prefabs. It lives in its own file because Unity can only save a MonoBehaviour
    /// into a prefab when the class is in a file with the same name.
    /// </summary>
    public class TestBehaviour : MonoBehaviour
    {
    }
}
